import { SentensecasepipePipe } from './sentensecasepipe.pipe';

describe('SentensecasepipePipe', () => {
  it('create an instance', () => {
    const pipe = new SentensecasepipePipe();
    expect(pipe).toBeTruthy();
  });
});
